# Project Rules

- Prefer TDD whenever possible.
- We handle warnings as soon as they appear.

## Git Protocol Constraints
- No fast-forward merges without explicit user approval (`--no-ff`).
- No branch or worktree deletion without explicit user approval.
- No squashing commits without explicit user approval.
- Always pause for user inspection (dry-run or staged-only merge) before committing a merge.
